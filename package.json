{
  "name": "my-revenge-plugins",
  "version": "1.0.0",
  "devDependencies": {
    "@revenge-mod/types": "latest",
    "esbuild": "latest"
  },
  "scripts": {
    "build": "esbuild plugins/*/index.ts --bundle --outdir=dist"
  }
}
